---
name: agents-notebook-skill
description: >
  使用此技能保存对话结束后会丢失的信息。当用户想要将某些内容记录下来以供未来参考时触发——
  可以是显式的（"记住"、"记一下"、"帮我记"、"写下来"、"别忘了"、"得记下来"、
  "remember"、"note that"、"save this"），也可以是隐式的（用户陈述了值得跨会话保留的
  重要事实、纠正、偏好、约束或发现）。包括：用户偏好和环境配置、领域纠正、通过测试发现的
  未记录行为、项目约束，以及任何用户标记为重要的事实。当用户要求回忆之前保存的信息时同样触发。
  不适用于：可从文件读取的代码结构、git 历史、调试日志或可从代码库推导的任何内容。
---

# agents-notebook-skill

持久化知识笔记本 —— 跨对话记录重要事实、决策和发现。

## 核心决策：记还是不记？

当从任何来源获得新的事实性信息时，问自己三个问题：

| 问题 | 含义 | "是"的例子 | "否"的例子 |
|------|------|-----------|-----------|
| **持久性？** | 未来会话中仍然有用？ | 架构决策 | "正在调试 X" |
| **独特性？** | 无法轻松重新推导？ | 通过测试发现的外部 API 限制 | 认证中间件位于 `src/auth.ts` |
| **可操作性？** | 影响决策、代码或理解？ | "必须支持 Python 3.8+" | 常见 git 命令语法 |

**三个都是"是" → 记录。** 任何一个"否" → 跳过。拿不准时倾向记录。

### 该记什么

- **用户偏好**：编码风格、工具选择、沟通偏好
- **项目约束**：版本要求、部署限制、合规规则
- **外部事实**：API 限制、库的特殊行为（通过测试或文档发现的）
- **关键决策**：选了什么、否决了什么、原因
- **领域洞察**：公式、阈值、代码中不明显的关系
- **纠正**：用户指出你搞错了什么 —— 同时记录纠正内容和它为什么重要

### 不该记什么

- 代码结构、模式或位置 —— 直接读代码即可
- Git 历史或最近变更 —— 用 `git log` / `git blame`
- 临时调试状态 —— 太短暂了
- 项目文档或 CLAUDE.md 中已有的内容
- 教科书或教程中的常识

## 写入前：读取 & 协调

笔记本不是只能追加的日志。添加新笔记前，先读取相关主题文件，检查：

1. **重复？** —— 已经记录？跳过。
2. **冲突？** —— 与已有矛盾？更新旧笔记，标记为已取代。
3. **精确化？** —— 是已有内容的更精确版本？就地更新。
4. **新主题？** —— 没有合适的文件？创建新的。

目标：每次读取都得到单一、连贯、无矛盾的全景图。

## 怎么写

笔记本路径：`.agents-notebooks/<主题词>.md`（默认平铺，一个主题 5+ 条笔记时用子目录）。用户指定了路径则用用户的。

```markdown
# 主题标题

## YYYY-MM-DD —— 简短标题

清晰简洁的陈述。最多 1-3 句。

来源：[用户告知 / 自行查阅 / 推理 / 归纳总结]
```

文件命名、子目录规则、INDEX.md 格式：见 [references/file-structure.md](references/file-structure.md)。

## 首次注册

在项目中创建第一条笔记时，在 `AGENTS.md` 和 `CLAUDE.md` 中注册。检测标记：`<!-- notebook-skill-registered -->`，如已存在则跳过。模板和插入规则：见 [references/registration-template.md](references/registration-template.md)。

## 参考资料

- [references/file-structure.md](references/file-structure.md) — 目录布局、命名规范、INDEX.md 格式、笔记格式模板
- [references/registration-template.md](references/registration-template.md) — 项目注册模板和插入规则
- [references/evaluation-guide.md](references/evaluation-guide.md) — 详细评估标准和边界情况
