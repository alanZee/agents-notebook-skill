---
name: agents-notebook-skill
description: >
  使用此技能保存对话结束后会丢失的信息。当用户想要将某些内容记录下来以供未来参考时触发——
  可以是显式的（"记住"、"记一下"、"帮我记"、"写下来"、"别忘了"、"得记下来"、
  "remember"、"note that"、"save this"），也可以是隐式的（用户陈述了值得跨会话保留的
  重要事实、纠正、偏好、约束或发现）。包括：用户偏好和环境配置、领域纠正、通过测试发现的
  未记录行为、项目约束，以及任何用户标记为重要的事实。当用户要求回忆之前保存的信息时同样触发。
  不适用于：可从文件读取的代码结构、git 历史、调试日志或可从代码库推导的任何内容。
---

# 笔记本（Notebook）

一个持久化的 markdown 笔记目录，用于跨对话捕获重要信息，防止有价值的知识因上下文窗口满溢或会话结束而丢失。

## 为什么需要这个

对话是短暂的。重要的事实 —— 用户的偏好、发现的 API 约束、关键的领域洞察 —— 都可能在上下文窗口填满或会话结束时消失。笔记本提供了一个轻量级、始终在线的捕获机制，确保有价值的信息不会溜走。

## 快速开始

0. **首次在项目中使用**：在 `AGENTS.md` 和 `CLAUDE.md` 中注册笔记本（见[首次注册](#首次注册)）
1. **找到或创建笔记本**：默认使用当前工作目录下的 `.agents-notebooks/`。需要时可加子目录如 `.agents-notebooks/<主题>/` 来组织
2. **学到新东西时**：评估持久性、独特性、可操作性（见下方）
3. **写入前**：读取相关主题文件，检查是否与已有内容重复或冲突
4. **写入或更新**：添加笔记，或与已有内容合并/更新
5. **需要过去的知识时**：从笔记本读取，优先查看 `INDEX.md`

## 核心决策：记还是不记？

当从任何来源（用户告知、自行查阅、推理、归纳总结）获得新的事实性信息时，问自己三个问题：

| 问题 | 含义 | "是"的例子 | "否"的例子 |
|------|------|-----------|-----------|
| **持久性？** | 未来会话中仍然有用？ | 架构决策 | "正在调试 X" |
| **独特性？** | 无法轻松重新推导？ | 通过测试发现的外部 API 限制 | 认证中间件位于 `src/auth.ts` |
| **可操作性？** | 影响决策、代码或理解？ | "必须支持 Python 3.8+" | 常见 git 命令语法 |

**三个都是"是" → 记录。** 任何一个"否" → 跳过。拿不准时倾向记录 —— 一条轻量笔记代价很低，但丢失一个关键洞察可能代价高昂。

### 该记什么

- **用户偏好**：编码风格、工具选择、沟通偏好
- **项目约束**：版本要求、部署限制、合规规则
- **外部事实**：API 限制、库的特殊行为（通过测试或文档发现的）
- **关键决策**：选了什么、否决了什么、原因
- **领域洞察**：公式、阈值、代码中不明显的关系
- **纠正**：用户指出你搞错了什么 —— 同时记录纠正内容和它为什么重要

### 不该记什么

- 代码结构、模式或位置 —— 直接读代码即可
- Git 历史或最近变更 —— 用 `git log` / `git blame`
- 临时调试状态 —— 太短暂了
- 项目文档或 CLAUDE.md 中已有的内容
- 教科书或教程中的常识

## 写入前：读取 & 协调

笔记本不是只能追加的日志。添加新笔记前，先读取相关主题文件（或扫描 `INDEX.md` 定位），检查：

1. **重复？** —— 这个事实是否已经记录？如果是，跳过。不要创建冗余条目。
2. **冲突？** —— 是否与已有笔记矛盾？如果是，用新信息更新旧笔记，并标记为已取代（如在标题后追加"（更新于 YYYY-MM-DD）"）。矛盾的笔记本比没有笔记本更糟糕。
3. **精确化？** —— 这是否是已有内容的更精确版本（如模糊偏好变成了具体要求）？如果是，就地更新已有笔记，而不是新增一条。
4. **新主题？** —— 如果没有合适的已有文件，创建新的。

目标：每次读取笔记本都应得到一个单一、连贯、无矛盾的全景图 —— 而不是一堆重叠条目的时间线堆砌。

## 首次注册

在项目中创建第一条笔记时，将笔记本注册到 `AGENTS.md` 和 `CLAUDE.md`，以便未来的会话能发现它。每个项目只执行一次。

### 检测

在文件内容中搜索精确标记 `<!-- notebook-skill-registered -->`。如果在任一文件中找到，跳过该文件 —— 已注册。

### 模板

插入以下内容块（精确内容，不可修改）：

```markdown
<!-- notebook-skill-registered -->

## Notebook

- **Skill**: `agents-notebook-skill` — 持久化知识笔记本，跨对话记录重要事实、决策和发现
- **笔记目录**: `.agents-notebooks/`（当前工作目录下）
- **用途**: 记录用户偏好、项目约束、外部事实、关键决策、领域洞察等
- **使用**: 当获得值得记住的新信息时自动触发；也可手动触发记录或回忆已有笔记
```

### 插入规则

1. 文件不存在 → 创建文件，仅包含上述模板。
2. 文件末尾有 `---` 分隔符 → 在分隔符**之前**插入模板，前后各留空行。
3. 其他情况 → 在文件末尾**追加**模板，与已有内容之间留一空行。
4. **绝不修改已有内容** —— 只做追加/插入。

*完整模板与示例：[references/registration-template.md](references/registration-template.md)*

## 怎么写笔记

### 文件结构

```
.agents-notebooks/
  INDEX.md              # 每个文件一行摘要（笔记本增长后维护）
  user-preferences.md   # 按主题组织的文件
  api-constraints.md
  fluid-sim/            # 复杂主题的子目录（5+ 条笔记时使用）
    numerics.md
    boundary-conditions.md
```

**文件命名**：`<主题词>.md` —— 小写、连字符、有描述性。

**子目录**：默认平铺。仅当一个主题自然分为 5+ 个独立笔记时才加子目录。不要过早过度组织。

### 笔记格式

```markdown
# 主题标题

## YYYY-MM-DD —— 简短描述性标题

清晰简洁地陈述事实或洞察。包含足够上下文让未来的自己理解为什么它重要，但不要重复代码库中已有的内容。

来源：[用户告知 / 自行查阅 / 推理 / 归纳总结]
```

每条笔记保持可快速扫描 —— 1-3 句话。5 秒内无法扫视完的笔记太长了。

### INDEX.md 格式

当笔记本超过 10 个文件时，维护一个索引：

```markdown
# 笔记本索引

- user-preferences.md — 编码风格、工具选择、沟通偏好
- api-constraints.md — 开发过程中发现的外部 API 限制和特殊行为
- fluid-sim/ — 数值方法、边界条件、湍流模型
```

## 笔记本位置策略

| 场景 | 位置 | 原因 |
|------|------|------|
| 默认 | 当前工作目录下的 `.agents-notebooks/` | 与项目共处，方便查找 |
| 用户指定 | 用户说的任何位置 | 始终尊重用户偏好 |

## 维护笔记本健康

- **更新过时笔记**：发现笔记过时了就修正或删除。错误的笔记比没有笔记更糟糕。
- **合并相关笔记**：两个笔记覆盖同一主题时，合并它们。
- **适时清理**：长期项目中，定期回顾并移除不再相关的笔记。

---

*详细的评估标准和边界情况，参见 [references/evaluation-guide.md](references/evaluation-guide.md)。*
